# Task Manager - Server

Server is built with Node.js, Express and MongoDB (Mongoose). It provides REST APIs for authentication, projects and tasks with JWT authentication.

Setup

1. Copy `.env.example` to `.env` and fill values.
2. Install dependencies:

```
cd server
npm install
```

3. Run in development:

```
npm run dev
```

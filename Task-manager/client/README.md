# Task Manager - Client

Frontend built with React + Vite + Tailwind.

Setup

```
cd client
npm install
npm run dev
```

Configure API base URL via `VITE_API_URL` in `.env` (defaults to http://localhost:5000/api)
